import { CategoryProductsPipePipe } from './category-products-pipe.pipe';

describe('CategoryProductsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryProductsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
